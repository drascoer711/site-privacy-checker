const MAX_BYTES=1500000,TIMEOUT=8000;
const TRACKERS=[['Google Analytics',/google-analytics|googletagmanager|gtag\(/i,'medium'],['Meta Pixel',/connect\.facebook\.net|fbq\(/i,'medium'],['TikTok Pixel',/analytics\.tiktok\.com|ttq\./i,'medium'],['Hotjar',/static\.hotjar\.com|hj\(/i,'medium'],['Microsoft Clarity',/clarity\.ms|clarity\(/i,'medium'],['Session replay',/fullstory|smartlook|mouseflow|logrocket/i,'high'],['Fingerprinting library',/fingerprintjs|fingerprint\.com|clientjs/i,'high']];
const FINGERPRINT=[['Canvas fingerprinting',/toDataURL\s*\(|getImageData\s*\(/i],['WebGL renderer detection',/WEBGL_debug_renderer_info|UNMASKED_RENDERER_WEBGL/i],['Audio fingerprinting',/OfflineAudioContext|AudioContext/i],['Battery status access',/navigator\.getBattery/i],['Hardware/device hints',/hardwareConcurrency|deviceMemory|maxTouchPoints|screen\.colorDepth/i]];
const TECH=[['WordPress',/wp-content|wp-includes/i],['Shopify',/cdn\.shopify\.com|shopifycdn/i],['React',/__next_data__|react(?:\.production)?\.min\.js/i],['Next.js',/_next\/static|__next_f/i],['Vue',/vue(?:\.runtime)?\.min\.js/i],['Bootstrap',/bootstrap(?:\.min)?\.(?:css|js)/i],['Google Tag Manager',/googletagmanager|gtm\.js/i],['Cloudflare',/cloudflare/i]];
const SECURITY=['content-security-policy','referrer-policy','permissions-policy','strict-transport-security','x-content-type-options','x-frame-options'];
import {saveReport} from '../lib/report-store.js';
function privateHost(h){h=h.toLowerCase();if(h==='localhost'||h.endsWith('.localhost')||h==='::1'||h==='0.0.0.0'||h.includes(':'))return true;if(/^(10|127)\./.test(h)||/^192\.168\./.test(h)||/^169\.254\./.test(h))return true;const m=h.match(/^172\.(\d+)\./);return !!(m&&+m[1]>=16&&+m[1]<=31)}
function target(value){let u;try{u=new URL(value.includes('://')?value:`https://${value}`)}catch{throw Error('Enter a valid website URL.')}if(!['http:','https:'].includes(u.protocol)||u.username||u.password||privateHost(u.hostname))throw Error('Only public HTTP(S) URLs without credentials can be scanned.');u.hash='';return u}
function cookies(list){return list.map(raw=>{const p=String(raw).split(';').map(x=>x.trim()),pair=p.shift()||'',i=pair.indexOf('='),name=i<0?pair:pair.slice(0,i);return{name:name||'Unnamed cookie',value:i<0?'':pair.slice(i+1,41),attributes:p.slice(0,8)}})}
function risk(fs){const score=fs.reduce((n,f)=>n+(f.severity==='high'?3:f.severity==='medium'?2:1),0);return{score,level:score>=7?'high':score>=3?'medium':'low'}}
export default async function handler(req,res){if(req.method!=='POST')return res.status(405).json({error:'Use POST.'});try{const u=target(req.body?.url||'');const c=new AbortController(),timer=setTimeout(()=>c.abort(),TIMEOUT);let r;try{r=await fetch(u,{signal:c.signal,redirect:'manual',headers:{'user-agent':'Tracecheck/1.0 (privacy audit)'}})}finally{clearTimeout(timer)}const type=r.headers.get('content-type')||'';if(!type.includes('text/html'))return res.status(422).json({error:'The URL did not return an HTML page.'});let html='',bytes=0,reader=r.body?.getReader();if(reader){const dec=new TextDecoder();while(true){const x=await reader.read();if(x.done)break;bytes+=x.value.byteLength;if(bytes>MAX_BYTES)break;html+=dec.decode(x.value,{stream:true})}}else html=await r.text();const findings=[];for(const [name,pattern,severity] of TRACKERS)if(pattern.test(html))findings.push({category:'Tracker',name,severity,detail:'A matching script or API pattern was found in page source.'});for(const [name,pattern] of FINGERPRINT)if(pattern.test(html))findings.push({category:'Fingerprinting',name,severity:'high',detail:'A browser/device identification API pattern was found in page source.'});const setCookies=r.headers.getSetCookie?.()||(r.headers.get('set-cookie')?[r.headers.get('set-cookie')]:[]),ck=cookies(setCookies);for(const x of ck)if(/track|analytics|_ga|fbp|pixel/i.test(x.name))findings.push({category:'Cookie',name:x.name,severity:'medium',detail:`Cookie attributes: ${x.attributes.join(', ')||'none'}`});const headers={};for(const h of SECURITY)headers[h]=r.headers.get(h)||null;const domains=[...new Set([...html.matchAll(/(?:src|href|action)=["']([^"']+)["']/gi)].map(m=>{try{return new URL(m[1],u).hostname}catch{return null}}).filter(x=>x&&x!==u.hostname))].slice(0,40);const technologies=TECH.filter(([,p])=>p.test(html)).map(([n])=>n);const present=SECURITY.filter(h=>headers[h]);const result={id:crypto.randomUUID(),url:u.href,status:r.status,truncated:bytes>MAX_BYTES,risk:risk(findings),findings,externalDomains:domains,technologies,cookies:ck,headers,security:{present:present.length,total:SECURITY.length,score:Math.round(present.length/SECURITY.length*100),missing:SECURITY.filter(h=>!headers[h])},fetchedAt:new Date().toISOString(),note:'HTML and response headers were inspected; JavaScript was not executed.'};const saved=await saveReport(result);return res.status(200).json({...saved,reportId:saved.id})}catch(e){return res.status(400).json({error:e.name==='AbortError'?'The scan timed out.':e.message||'The scan failed.'})}}
